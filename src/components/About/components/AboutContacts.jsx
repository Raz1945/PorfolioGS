import { ContactsWrapper, DownloadLink, SocialLinksList } from './AboutContacts_components';

export const AboutContacts = () => {
  return (
    <ContactsWrapper>
      <SocialLinksList />
      <DownloadLink />
    </ContactsWrapper>
  );
}

